from setuptools import setup, find_packages

setup(
    name='itu_auction',  # Name of your project
    version='0.1',       # Version of your project
    packages=find_packages(),  # Automatically find packages
)
